# 🚀 Hugo博客部署到GitHub Pages

## 📋 前置条件
- 已安装Git
- 拥有GitHub账号
- 已安装Hugo（本机已有）

---

## 🎯 三步快速部署

### 第1步：创建GitHub仓库
1. 访问 https://github.com/new
2. **仓库名必须是**：`你的用户名.github.io`
   - 例如：zhangsan.github.io
3. 选择 **Public** 公开仓库
4. **不要**勾选Initialize with README
5. 点击"Create repository"

### 第2步：修改配置文件
打开 `hugo.yml` 第1行，修改为：
```yaml
baseURL: "https://你的用户名.github.io"
```

### 第3步：推送代码到GitHub
打开命令行，执行：

```bash
# 1. 初始化git（如果还没有）
git init

# 2. 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/你的用户名.github.io.git

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "Initial commit"

# 5. 推送到GitHub
git push -u origin main
```

✅ **完成！** 等待5-10分钟后访问：`https://你的用户名.github.io`

---

## 🔄 后续更新部署

每次修改内容后，只需执行：
```bash
git add .
git commit -m "更新内容"
git push
```

**自动部署已配置**：推送到GitHub后会自动构建和部署，无需手动运行hugo命令。

---

## 🎨 部署后必做的个性化操作

### 1. 修改博客基本信息
编辑 `hugo.yml`：
```yaml
title: "你的博客标题"
description: "你的博客描述"
author: "你的名字"

# 修改社交链接
links:
  - name: "GitHub"
    url: "https://github.com/你的用户名"
  - name: "Email"
    url: "mailto:你的邮箱@example.com"
```

### 2. 替换头像
- 准备你的头像图片（建议200x200像素）
- 重命名为 `avatar.svg` 或 `avatar.png`
- 放置到 `static/img/` 目录
- 推送更新：
  ```bash
  git add static/img/avatar.svg
  git commit -m "更新头像"
  git push
  ```

### 3. 修改"关于我"页面
编辑 `content/about.md`，填写个人信息后推送更新。

### 4. 添加新文章
```bash
# 创建新文章
hugo new posts/文章标题.md

# 编辑 content/posts/文章标题.md
# 添加完毕后推送
git add .
git commit -m "发布新文章：文章标题"
git push
```

### 5. 本地预览
在推送前可以先本地预览：
```bash
hugo server -D
```
访问 http://localhost:1313 查看效果

---

## 📱 可选的高级个性化

### 自定义域名
1. 在域名DNS中添加CNAME记录：
   - 主机记录：`www` 或 `@`
   - 记录类型：`CNAME`
   - 记录值：`你的用户名.github.io`

2. 在项目根目录创建 `static/CNAME` 文件：
   ```
   www.yourdomain.com
   ```

3. 推送更新

### 添加网站分析
在 `layouts/_default/baseof.html` 的 `<head>` 标签内添加：

**百度统计：**
```html
<script>
var _hmt = _hmt || [];
(function() {
  var hm = document.createElement("script");
  hm.src = "https://hm.baidu.com/hm.js?你的统计代码";
  var s = document.getElementsByTagName("script")[0];
  s.parentNode.insertBefore(hm, s);
})();
</script>
```

**Google Analytics：**
```html
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

### 添加评论功能
Hugo是静态站点，可以使用第三方评论服务：
- **Gitalk**：基于GitHub Issues，推荐
- **Valine**：基于LeanCloud
- **Waline**：Valine的升级版
- **Giscus**：基于GitHub Discussions

选择一个服务，按照其文档配置即可。

---

## ⚠️ 常见问题

### Q1: 部署后显示404？
**解决方案：**
1. 确认仓库名正确（必须为 `用户名.github.io`）
2. 确认baseURL配置正确
3. 检查GitHub仓库的Settings > Pages设置
4. 等待5-10分钟让GitHub完成部署

### Q2: 部署后样式错乱？
**解决方案：**
1. 检查baseURL配置是否正确
2. 等待CDN刷新（可能需要几分钟）
3. 清除浏览器缓存

### Q3: 如何回滚到上一个版本？
```bash
cd public
git log  # 查看历史
git reset --hard HEAD^
git push origin main --force
```

### Q4: 如何添加更多页面？
```bash
hugo new about/page-name.md
hugo new links/links.md
```
页面会创建在 `content/` 对应目录下。

### Q5: 首屏Hero每次都显示怎么办？
已配置localStorage，首次访问后不再显示。如果想重新看到：
- 浏览器控制台执行：`localStorage.removeItem('blogVisited'); location.reload();`

---

## 📚 参考资源

- **Hugo官方文档**：https://gohugo.io/documentation/
- **Blowfish主题**：https://nunocoracao.github.io/blowfish/
- **GitHub Pages**：https://docs.github.com/pages
- **Font Awesome图标**：https://fontawesome.com/search

---

## 💡 快速命令参考

```bash
# 本地预览
hugo server -D

# 构建站点
hugo --minify

# 创建新文章
hugo new posts/文章名.md

# 创建新页面
hugo new about/页面名.md

# 提交更新
git add .
git commit -m "提交说明"
git push

# 查看Hugo版本
hugo version
```

---

## ✅ 部署检查清单

- [ ] GitHub仓库创建完成（仓库名：用户名.github.io）
- [ ] hugo.yml的baseURL已修改
- [ ] 代码已推送到GitHub
- [ ] GitHub Pages已启用（Settings > Pages）
- [ ] 网站可正常访问
- [ ] 修改了博客标题和描述
- [ ] 替换了头像图片
- [ ] 修改了"关于我"页面
- [ ] 发布了第一篇文章

**祝你部署成功！🎉**
